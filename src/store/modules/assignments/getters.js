export default {
    getAssignments(state) {
        return state.assignments;
    },
    getAssignment(state) {
        return state.assignment;
    },
    toggleModal(state) {
        return state.toggleModal;
    },
};
